import styled from 'styled-components/native';
import {space, color, typography, size, borderRadius} from 'styled-system';

const Input = styled.TextInput`
  ${space}
  ${color}
  ${typography}
  ${size}
  ${borderRadius}
`;

export default Input;
